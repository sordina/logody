---
# foo:
#   process: uname
#   resume:
#     - succeed

bar:
  shell: "{ for i in `seq 1 10`; do echo bar && sleep 1 } ; done && exit 1"
  resume:
    - fail

      # baz:
      #   shell: "echo bla"
      #   resume:
      #     - succeed
      #     - fail
